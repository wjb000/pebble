# Archived STLs — do **not** print for HouseHand v3

Kept for license provenance / history only.

| Folder | Why archived |
|--------|----------------|
| `xlerobot/` | Fragmented XLe 035 decks, RÅSKOG cart armbase, unused gimbal plate, old neck |
| `base/` | Legacy perceptron chassis |
| `lift/` | Prusa Z / exposed-rail leftovers (v3 uses torso column) |
| `lekiwi/` | `modified_base_arm.stl` — onboard single-arm adapter (product is dual SO-101 on HouseHand deck) |

Active kit: `print/README.md`.
