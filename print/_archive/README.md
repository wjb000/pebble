# Print archive / optional

**Do not print these for the default HouseHand robot.** Active kit: `print/README.md`.

| Path | Why archived |
|------|----------------|
| `lekiwi/` | Wrong motor mounts (`_v2`, B/C), LeKiwi-arm wrist cam, Jetson holder |
| `SO101/leader/` | Leader-only SO-101 parts (`Trigger`, `Handle`, `Wrist_Roll`) — not for followers |
| `optional/head_soarm_overhead/` | Alternate SO-ARM overhead cam stack (not the twin head) |
| `optional/lift_4040/` | Optional 4040 bar mounts (not in twin) |
| `optional/base_stub/` | Empty legacy base stub docs |
| `xlerobot/` | Old XLe 035 decks / RÅSKOG armbase / lattice neck |

If you intentionally want a leader arm, overhead cam, or 4040 lift, pull from here — not from the master checklist.
